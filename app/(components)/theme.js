// theme.js
import { createTheme } from '@mui/material/styles';
import '@fontsource/roboto'; // Import font (Roboto as example)

const theme = createTheme({
  typography: {
    fontFamily: 'Roboto, Arial, sans-serif',
  },
});

export default theme;
